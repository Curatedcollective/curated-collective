server {
  listen 80;
  server_name curatedcollective.social www.curatedcollective.social;

  location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Redirect ACME challenge to certbot if needed
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }
}

# After certbot, enable SSL block (example)
# server {
#   listen 443 ssl;
#   server_name curatedcollective.social www.curatedcollective.social;
#   ssl_certificate /etc/letsencrypt/live/curatedcollective.social/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/curatedcollective.social/privkey.pem;
#   include /etc/letsencrypt/options-ssl-nginx.conf;
#   ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#   location / {
#     proxy_pass http://127.0.0.1:8080;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#}
